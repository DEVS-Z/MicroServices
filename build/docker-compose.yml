services:
  app:
    container_name: base-droply
    image: ghcr.io/oppendevs/servicio-base-de-droply:v1.1.0
    volumes:
      - 
    command: go run ./cmd/rest/main.go
    ports:
      - "80:8080"

      
